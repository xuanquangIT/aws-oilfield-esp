# Architecture assets

[offshore-esp-aws-architecture.svg](offshore-esp-aws-architecture.svg) is the high-level system diagram. It uses the AWS Architecture Icons package downloaded on 2026-09-05 from the [official AWS icon page](https://aws.amazon.com/architecture/icons/).

The selected icon SVG files are retained in `aws-icons/` so the diagram remains self-contained. Refresh icons only when intentionally updating the diagram, using the current package from that page.
